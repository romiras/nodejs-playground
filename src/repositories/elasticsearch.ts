import { TStatusID, TStatus } from "../types";

export function GetStatusesByIDs(ids: Array<TStatusID>): Array<TStatus> {
	// TODO implement GetStatusesByIDs

    let statuses = Array<TStatus>();

    return statuses;
};
